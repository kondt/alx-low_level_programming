#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase except e and q, then a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int count;

	count = 96;

	while (n++ <= 121)
	{
		if (n != 101 && n != 113)
			putchar(count);
	}
	return (0);
}
